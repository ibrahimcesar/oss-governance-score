- **A1 mislabels a part–whole check as a "size baseline".** S0 = f(log contributors_5plus, HHI) uses two catalogue inputs (D4, D2); ρ(S0, sub_D2) = 0.96. It restates the existing D1+D3+D5 circularity check (ρ = 0.824, already in 4.2). Its numbers do not reproduce: LOO fit gives S0 means 78/66/42/35 (ρ = 0.89), not 82/63/34/23; the "Federation–Club gap collapses from 19 to −6" sentence is garbled (score gap is +5.7; S0 gap +12; no sign reversal). The decisive number is omitted: ρ(S0, Scorecard) = 0.64 vs 0.75 — and the report then forbids the Steiger test that would compare them, while citing the study's own Steiger at n = 53.
- **A1's CI is inconsistent with its conclusion**: bootstrap [0.2, 0.75] reported, then "roughly 0.4–0.7" stated.
- **A2 and A4 interact and the report misses it**: `issue_template` is False for 95/100 by defect, so it is the largest single "gaming" lever (2.78 pts × 95 repos); the envelope inflates a measurement error into gameability. Medians reproduce only approximately (mine: toy 25.4 / stadium 16.9 / club 14.1 / fed 13.9).
- **A3 noise table mixes units**: weights at ±25% (0.998) next to thresholds at ±50% joint (0.989); the ±25% threshold figure is 0.999. It also ignores that cap4's validation table already carries 95% CIs. **Contradiction**: A3 says drop the 88.8% non-overlap statistic; CT note §4 keeps it as the invariant.
- **A5**: "8/93" — all 100 sample repos have commit caches; denominator unexplained. golang/go "100% Gerrit trailers" is measured on a 1,700-commit truncated cache; linux/FFmpeg are already D3-NaN, so the gate touches ≤ 6 repos. Re-scoring observed zeros (git/git review coverage 0.25, merge 0.00) as NaN is a post hoc catalogue change that raises those scores — needs a v2 decision record, and "expecting no detectable change" has no number.
- **CT §2 factorial rebuttal is wrong**: cells use different cut-points (active ≥100 vs ≥20; ≤10 vs ≤3; stars ≥10k / 500–5k / <500), so "users_high × contrib_high" is not a balanced 2×2 with constant factor levels; the "confounded by construction" verdict stands.
- **CT §1 overclaims a "law"**: silence pays in 22/96 (reproduced), a conditional, not a design law.
- **CT §3 / report §6.5**: "counts compose" is false for contributor counts (overlap across windows); only commit/release counts are cosheaf-typed. Ȟ¹ "= 0" for a Set-valued sheaf is a pointed-set statement, not zero.
- **CT bridge item 4 names the only runnable check and never runs it**: govscore data give entropy Fed 0.695 > Stadium 0.425 — the arrow Federation→Stadium fails here too.
- **Open critiques untouched by Tier A** despite "cover what will be asked": 13 (club sub-band), 14 (language breakdown), 17 (check-level overlap — B2 depends on it, yet it costs zero new data), D2/D4 fusion (ρ = 0.87; CT §3 offers mechanism only).
- **Cheap offline audits not proposed**: bot-heuristic error rate on cached logins (limitation iv); noreply e-mails vs elephant-factor generic-domain rule.
- **No wording fix for the title/artifact mismatch** ("padrões" vs compensatory score) beyond A4.
- **Unverified claims**: "no empirical test of the typology as trajectories exists"; "11% of toy response edges in window"; all probabilities (0.35/0.5/0.2) are invented. Linux Scorecard 3.0 is correct.
- **Guardrails**: Tier C threshold re-derivation self-flagged; A4's "repair via 300 API calls" violates never-reconsult — correctly noted.